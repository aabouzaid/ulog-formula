ulog:
  pkgs:
    - ulogd2

  service:
    name  : ulogd2

  log:
    file  : "syslog"
    level : 3

  plugins: []

  # Ulog will fail if it has no "sections" config.
  # Sections config should be in pillar.
  sections: {}
