ulog:
  pkgs:
    - ulogd2

  service:
    name  : ulogd2

  log:
    file  : "syslog"
    level : 3

  plugins: []

  # Ulog will fail if "sections" has no config inside it.
  # Sections config should be in pillar.
  sections: {}
